{
  "name": "app-common",
  "description": "Common application code that both app-worker and app-window need to use",
  "bangleWorkspaceConfig": {
    "allowedWorkspaces": [
      "js-lib",
      "lib",
      "shared"
    ]
  }
}
