# Whitespace and line breaks

These lines form
one soft-wrapped paragraph.

This line uses a backslash hard break.\
The next line must remain separate.

This line uses an explicit HTML break.<br>
The following text comes after it.

Paragraph after several blank lines.



Final paragraph with leading inline spaces preserved inside `  code  `.
