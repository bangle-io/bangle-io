[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

[build]
  publish = "packages/tooling/browser-entry/dist"
  command = "pnpm build" 

[build.environment]
  NODE_VERSION = "lts/*"
