[[headers]]
  for = "/manifest.webmanifest"
  [headers.values]
    Content-Type = "application/manifest+json"

[build]
  publish = "packages/entry/browser-entry/dist"
  command = "pnpm build" 

[build.environment]
  NODE_VERSION = "lts/*"
